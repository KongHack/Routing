services:
  php:
    volumes:
      - type: bind
        source: '${HOME}/.config/composer/auth.json'
        target: '/tmp/routing-home/.composer/auth.json'
        read_only: true
      - type: bind
        source: '${HOME}/.ssh'
        target: '/tmp/routing-home/.ssh'
        read_only: true
